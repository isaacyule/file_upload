<h1>User Index</h1>

<% @users.each do |user| %>
  <div class="row">
    <div class="col-xs-12">
      <h4><%= link_to "#{user.name}", user_path(user) %></h4>
      <p><i><%= user.email %></i></p>
    </div>
  </div>
<% end %>
